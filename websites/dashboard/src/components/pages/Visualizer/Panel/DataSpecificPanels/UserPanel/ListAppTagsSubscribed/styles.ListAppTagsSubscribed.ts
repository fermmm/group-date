import styled from "styled-components";

export const TagListContainer = styled("div")`
   display: flex;
   flex-direction: row;
   flex-wrap: wrap;
   width: 100%;
   gap: 5px;
   padding-left: 25px;
`;
